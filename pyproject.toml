[project]
name = "e-commerce-simulation"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"

dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "pydantic-settings",
    "aio-pika",
    "pytest",
]
